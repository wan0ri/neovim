# Copy this file to `.env` and fill the values.

# Required for mcpServers.github
GITHUB_TOKEN=

# Optional for mcpServers.terraform (Terraform Cloud/Enterprise)
TERRAFORM_CLOUD_TOKEN=

# Required for context7 (local stdio or remote HTTP)
CONTEXT7_API_KEY=

# Optional: override allowed paths for filesystem server
# ALLOW=/Users/wan0ri:/Users/wan0ri/project

# Hint: Some MCP clients (e.g. Codex CLI) can read a servers file
# via an env variable. If supported in your version, export this:
# MCP_SERVERS_PATH=/Users/wan0ri/.config/mcphub/servers.json
